public class AVLTree {
	private static class Node {
        int data;
        int height;
        int balance;
        Node left;
        Node right;
        Node parent;

        Node(int data) {
            this.data = data;
            height = 1;
            balance = 0;
            left = null;
            right = null;
            parent = null;
        }
    }

